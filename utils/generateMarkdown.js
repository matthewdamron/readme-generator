// TODO: Create a function to generate markdown for README
function generateMarkdown(readmeData) {
  // console.log(readmeData);
  return `# ${readmeData.projectTitle}

`;
}

module.exports = generateMarkdown;